_cjs
_esm
_types
node_modules
.DS_STORE

dist

.env*

tsconfig*.tsbuildinfo

.vercel

# vitest coverage output
coverage

# vocs build output
docs/dist

# waku route types, regenerated by `vocs dev`/`vocs build`
docs/pages.gen.ts

# Local dev stack — work in progress, not part of this release. The directory
# stays on disk so it can be picked up again; see dev-stack/PARKED.md for what
# to restore when it ships.
dev-stack/
