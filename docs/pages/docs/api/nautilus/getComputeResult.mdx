# getComputeResult

Get a download URL for a finished job's result.

## Usage

```ts twoslash
import { Nautilus } from '@deltadao/nautilus'
import { Wallet } from 'ethers'
const nautilus = await Nautilus.create(new Wallet('0x1234'))
// ---cut---
const url = await nautilus.getComputeResult({ jobId: 'a-job-id' }) // [!code focus]

if (url) {
  const results = await fetch(url)
}
```

## Returns

- Type: `string | undefined`

`undefined` — with a log line explaining which — when the node does not know the job, the
job has not finished (status is not `70`), or it has no `output` result and no
`resultIndex` was given.

To stream a large result instead of downloading it through a URL, use
[`streamComputeResult`](/docs/api/nautilus/streamComputeResult).

## Parameters

### config

- Type: `ComputeResultConfig`

#### Properties

##### jobId

- Type: `string`

##### resultIndex (optional)

- Type: `number`

Which result to fetch. Defaults to the first result of type `output`. A job also produces
`algorithmLog`, `configrationLog` and `publishLog` entries.

##### nodeUri (optional)

- Type: `string`
