---
searchPriority: 0
---

:::warning
You are viewing the documentation for Nautilus **v1.1.0** (legacy). Switch to the [latest documentation](/docs/introduction), or follow the [migration guide](/docs/migration) to upgrade.
:::

# setType

Set the type of the asset. This might affect what kind of other metadata is expected for the asset to be valid.
See a usage example below for both supported cases: `'dataset'` and `'algorithm'`.

Official OceanProtocol documentation: [DDO Specification](https://docs.oceanprotocol.com/developers/ddo-specification)

## Usage

When defining `algorithm` type assets, additional metadata is necessary to be set. Feel free to toggle between the examples below or refer to the [`setAlgorithm() documentation`](/v1/docs/api/assetbuilder/setAlgorithm) for further information.

:::code-group
```ts [dataset]
import { AssetBuilder, Nautilus } from '@deltadao/nautilus'
import { Wallet } from 'ethers'

const nautilus = await Nautilus.create(new Wallet('0x1234'))

const assetBuilder = new AssetBuilder()
assetBuilder.setType('dataset') // [!code focus]
const asset = assetBuilder.build() // [!code focus]

await nautilus.publish(asset) // [!code focus]
// => ddo.metadata: 
// => { 
// =>    type: 'dataset'
// => }
```

```ts [algorithm]
import { AssetBuilder, Nautilus } from '@deltadao/nautilus'
import { Wallet } from 'ethers'

const nautilus = await Nautilus.create(new Wallet('0x1234'))

const assetBuilder = new AssetBuilder()
assetBuilder.setType('algorithm') // [!code focus]
assetBuilder.setAlgorithm({ // [!code focus]
    container: { // [!code focus]
        entrypoint: 'node $ALGO', // [!code focus]
        image: 'node', // [!code focus]
        tag: 'lts', // [!code focus]
        checksum: 'sha256:a6faa1aa0ae1981b70c075dd6ea0a1725a1d05a4cab85536460ae4e4710e8331' // [!code focus]
    }, // [!code focus]
}) // [!code focus]
const asset = assetBuilder.build() // [!code focus]

await nautilus.publish(asset) // [!code focus]
// => ddo.metadata: 
// => { 
// =>    type: 'algorithm'
// =>    algorithm: {
// =>        container: {
// =>            entrypoint: 'node $ALGO',
// =>            image: 'node',
// =>            tag: 'lts',
// =>            checksum: 'sha256:a6faa1aa0ae1981b70c075dd6ea0a1725a1d05a4cab85536460ae4e4710e8331'
// =>        }
// =>    }
// => }
```
:::

## Returns

- Type: [`AssetBuilder`](/v1/docs/api/AssetBuilder)

The asset builder instance to chain building calls with.

## Parameter

### name

- Type: `'dataset' | 'algorithm'`

The type to use for the asset's metadata.

Refer to the [`setAlgorithm() documentation`](/v1/docs/api/assetbuilder/setAlgorithm) to learn more about required metadata when publishing algorithm assets.

```ts
assetBuilder.setType(
    'dataset' // [!code focus]
)
```
